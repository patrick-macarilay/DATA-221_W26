Assignment1_Question1 contains a function that multiplies consecutive integers until its past a set threshold.
Assignment1_Question2 contains a function that displays the length and parity of a list of strings.
Assignment1_Question3 contains a function that computes x^y from a set list of given pairs.
Assignment1_Question4 contains a function that randomly assigns a value x in between 0 and 1, then finds if a random number will match it.
Assignment1_Question5 contains a function that finds the area of two circles, given their radii, then displays how much the smaller circle can cover the larger circle.
Assignment1_Question6 contains a function that returns each value is the percentage of elements in the list that are less thaj or equal to that key.
Assignment1_Question7 contains a function that reads a number of seconds and return how many hours, minutes, and seconds it is together.
Assignment1_Question8 uses the pandas library to display a dataframe of a set list of data and make another column derived from the existing columns.
